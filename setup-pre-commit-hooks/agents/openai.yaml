interface:
  display_name: "Setup Pre-commit Hooks"
  short_description: "Set up local checks before commit"
  default_prompt: "Use $setup-pre-commit-hooks to add or update repository-local pre-commit checks for formatting, linting, tests, or similar commit-time gates."
