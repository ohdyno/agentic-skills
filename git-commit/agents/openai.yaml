interface:
  display_name: "Git Commit"
  short_description: "Draft and run conventional commits"
  default_prompt: "Use $git-commit when I ask you to commit the current changes so you inspect the full change set, write one concise conventional commit message, and execute the git commit."
