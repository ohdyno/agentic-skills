interface:
  display_name: "Git Commit"
  short_description: "Draft and run conventional commits"
  default_prompt: "Use $git-commit to inspect the current change set, write an appropriate Conventional Commit, and run git commit."
