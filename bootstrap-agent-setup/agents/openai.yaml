interface:
  display_name: "Bootstrap Agent Setup"
  short_description: "Copy a standard agent scaffold into a repo"
  default_prompt: "Use $bootstrap-agent-setup to install this repo's standard agent scaffold into the current repository."
